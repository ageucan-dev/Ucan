// Endpoint antigo do formulário. Mantido por compatibilidade; o submit atual usa WhatsApp.
window.UCAN_FORM_ENDPOINT = "";

// Webhook seguro (ex.: n8n) para captura server-side/CAPI.
// Deixe vazio até configurarmos o endpoint. Nunca coloque token da Meta neste arquivo.
window.UCAN_TRACKING_ENDPOINT = "";
